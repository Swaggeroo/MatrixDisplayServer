openapi: 3.0.3
info:
  title: Matrix Display Server API
  description: API for managing and applying images to a matrix display.
  version: 0.6.1
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
servers:
  - url: http://localhost:3000
    description: Development server
  - url: https://matrix-display-server.lumo.local.swaggeroo.de
    description: Production server
security:
  - {} # No security by default
paths:
  /api/upload:
    post:
      operationId: uploadImage
      summary: Upload an image
      description: Uploads a PNG or GIF image to the server for processing and storage.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                pictures:
                  type: string
                  format: binary
                  description: The image file to upload (PNG or GIF).
                title:
                  type: string
                  minLength: 3
                  maxLength: 30
                  pattern: '^[a-zA-Z0-9 ]+$'
                  description: The title of the image (alphanumeric and spaces).
              required:
                - pictures
                - title
      responses:
        '200':
          description: File(s) uploaded successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: File(s) uploaded!
                  uuids:
                    type: array
                    items:
                      type: string
                      format: uuid
        '400':
          description: Invalid request.
          content:
            text/plain:
              schema:
                type: string
        '500':
          description: Server error.
          content:
            text/plain:
              schema:
                type: string

  /api:
    get:
      operationId: listImages
      summary: List all image UUIDs
      description: Returns a list of all processed image UUIDs.
      responses:
        '200':
          description: A list of UUIDs.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                  format: uuid
        '400':
          description: Bad Request.

  /api/picture/{uuid}:
    get:
      operationId: getPictureDetails
      summary: Get picture details
      description: Returns the URL and name of the image with the given UUID.
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Picture details found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    format: uri
                  name:
                    type: string
        '404':
          description: Picture not found.
        '500':
          description: Server error.

    delete:
      operationId: deletePicture
      summary: Delete a picture
      description: Deletes the image and its database entry for the given UUID.
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Image deleted successfully.
        '404':
          description: Picture not found.
        '500':
          description: Server error or file not found.

  /api/random:
    get:
      operationId: getRandomPicture
      summary: Get a random picture UUID
      description: Returns a random UUID from the list of processed images.
      responses:
        '200':
          description: A random UUID.
          content:
            text/plain:
              schema:
                type: string
                format: uuid
        '404':
          description: No images found.

  /api/apply/{uuid}:
    post:
      operationId: applyImage
      summary: Apply image to matrix
      description: Sends the image data to the matrix display.
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Streaming status of the application process.
          content:
            text/plain:
              schema:
                type: string
        '400':
          description: Another image is currently being applied.
        '404':
          description: Image not found.
        '500':
          description: Server error.

  /api/setBrightness:
    post:
      operationId: setBrightness
      summary: Set matrix brightness
      description: Updates the brightness of the matrix display.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                brightness:
                  type: integer
                  minimum: 1
                  maximum: 255
              required:
                - brightness
      responses:
        '200':
          description: Brightness set successfully.
        '400':
          description: Invalid brightness value.
        '500':
          description: Error communicating with the matrix.

  /api/setSpeed:
    post:
      operationId: setSpeed
      summary: Set animation speed
      description: Updates the animation speed (frame delay) of the matrix display.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                speed:
                  type: integer
                  minimum: 0
                  maximum: 60000
              required:
                - speed
      responses:
        '200':
          description: Speed set successfully.
        '400':
          description: Invalid speed value.
        '500':
          description: Error communicating with the matrix.

  /api/status:
    get:
      operationId: getStatus
      summary: Get server and matrix status
      description: Returns status of the application.
      responses:
        '200':
          description: Current status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  applyingImage:
                    type: boolean
                  brightness:
                    type: integer
                  speed:
                    type: integer
        '400':
          description: Bad Request.

  /api/off:
    post:
      operationId: turnOffMatrix
      summary: Turn off matrix
      description: Sends a command to turn off the matrix display.
      responses:
        '200':
          description: Matrix turned off.
        '400':
          description: Bad Request.
        '500':
          description: Error communicating with the matrix.

  /api/checkIntegrity:
    get:
      operationId: checkIntegrity
      summary: Check system integrity
      description: Synchronizes the database and the image files.
      responses:
        '200':
          description: Integrity check completed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: Cleaned
        '400':
          description: Bad Request.
        '500':
          description: Database connection issue.

  /pictures/{filename}:
    get:
      operationId: getStaticPicture
      summary: Serve static picture files
      description: Serves processed PNG or GIF files.
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: The image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
        '404':
          description: File not found.
